#include <stdio.h>

/**
 * main - entry point
 * function that takes a pointer and updates its values
 * Return: Always 0 (success)
 */
void reset_to_98(int *n)
{
int n = 98;
*ptr = n;
}

int main(void)
{
n = 402;
printf("n=%d", n);
reset_to_98(&n);
printf("n=%d", n);
return (0);

}
