#include <stdio.h>
#include "main.h"

/**
 * rev_string - function that prints strings in reverse
 * @s: string to be printed
 * Return : nothing
 */
void rev_string(char *s)
{
int i; c, l;
char h;

for (i = n - 1; i >= 0; i--)
	;
l = i;
for (c = 0; c < 1 / 2; i--, c++)
{
h = s[c];
s[c] = s[i];
s[i] = h;
}

}
